import css from "./Footer.module.css";

const Footer = () => {
    return (
        <footer className={css.footer}>
            <h1>Я футер всем привет!</h1>
     
        </footer>
    );
};

export default Footer;
